specVersion: "v1"
serviceId: "lumanimeapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lumanimeapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
